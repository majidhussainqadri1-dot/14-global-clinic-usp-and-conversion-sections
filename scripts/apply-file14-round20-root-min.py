from pathlib import Path
p=Path('14-global-clinic-usp-integration/includes/class-gcu-future-intelligence.php')
s=p.read_text()
def r(old,new,label):
    global s
    if s.count(old)!=1: raise SystemExit(f'{label}: {s.count(old)}')
    s=s.replace(old,new,1)
r("$id = isset( $data['report_id'] ) && wp_is_uuid( $data['report_id'] ) ? $data['report_id'] : wp_generate_uuid4();","$id = isset( $data['report_id'] ) && wp_is_uuid( $data['report_id'] ) ? sanitize_text_field( $data['report_id'] ) : wp_generate_uuid4();",'id sanitize')
r("\t\t$actor_hash = is_user_logged_in() ? GCU_Integrity::future_actor_hash( get_current_user_id() ) : null;\n\t\tglobal $wpdb; $t = self::tables();", "\t\t$actor_hash = is_user_logged_in() ? GCU_Integrity::future_actor_hash( get_current_user_id() ) : null;\n\t\t$identity = array( 'report_type'=>'copy_quality', 'route_key'=>$route, 'block_key'=>$block, 'locale'=>$locale, 'reason_code'=>$reason, 'message'=>$message, 'actor_hash'=>null === $actor_hash ? '' : $actor_hash );\n\t\tglobal $wpdb; $t = self::tables();", 'identity expected')
r("\t\tif(!$repo->begin_owned_transaction()){return new WP_Error('gcu_future_report_transaction_failed',__('The report transaction could not start.','global-clinic-usp-integration'),array('status'=>503));}\n\t\t$insert =", "\t\tif(!$repo->begin_owned_transaction()){return new WP_Error('gcu_future_report_transaction_failed',__('The report transaction could not start.','global-clinic-usp-integration'),array('status'=>503));}\n\t\t$wpdb->last_error='';$existing=$wpdb->get_row($wpdb->prepare(\"SELECT report_type,route_key,block_key,locale,reason_code,message,actor_hash FROM {$t['reports']} WHERE public_id=%s LIMIT 1\",$id),ARRAY_A);\n\t\tif(''!==(string)$wpdb->last_error){$repo->rollback_owned_transaction();return new WP_Error('gcu_future_report_identity_query_failed',__('The report identity could not be verified safely.','global-clinic-usp-integration'),array('status'=>503));}\n\t\tif($existing){if(!self::report_identity_matches($existing,$identity)){$repo->rollback_owned_transaction();return new WP_Error('gcu_future_report_identity_conflict',__('This report identifier is already bound to different report data.','global-clinic-usp-integration'),array('status'=>409));}if(!$repo->commit_owned_transaction()){$repo->rollback_owned_transaction();return new WP_Error('gcu_future_report_commit_failed',__('The report could not be committed safely.','global-clinic-usp-integration'),array('status'=>503));}return array('report_id'=>$id,'status'=>'open','deduplicated'=>true);}\n\t\t$wpdb->last_error='';\n\t\t$insert =", 'preinsert identity')
r("\t\tif(false===$insert){$repo->rollback_owned_transaction();return new WP_Error('gcu_future_report_write_failed',__('The report could not be recorded safely.','global-clinic-usp-integration'),array('status'=>500));}\n\t\tif(1===$insert)", "\t\tif(false===$insert||''!==(string)$wpdb->last_error){$repo->rollback_owned_transaction();return new WP_Error('gcu_future_report_write_failed',__('The report could not be recorded safely.','global-clinic-usp-integration'),array('status'=>500));}\n\t\tif(0===$insert){$wpdb->last_error='';$existing=$wpdb->get_row($wpdb->prepare(\"SELECT report_type,route_key,block_key,locale,reason_code,message,actor_hash FROM {$t['reports']} WHERE public_id=%s LIMIT 1\",$id),ARRAY_A);if(''!==(string)$wpdb->last_error||!$existing){$repo->rollback_owned_transaction();return new WP_Error('gcu_future_report_identity_query_failed',__('The report identity could not be verified safely.','global-clinic-usp-integration'),array('status'=>503));}if(!self::report_identity_matches($existing,$identity)){$repo->rollback_owned_transaction();return new WP_Error('gcu_future_report_identity_conflict',__('This report identifier is already bound to different report data.','global-clinic-usp-integration'),array('status'=>409));}}\n\t\tif(1===$insert)", 'postinsert identity')
helper="""\n\tprivate static function report_identity_matches( array $row, array $expected ) {\n\t\tforeach ( $expected as $key => $value ) { $stored=isset($row[$key])&&null!==$row[$key]?(string)$row[$key]:''; if(!hash_equals($stored,(string)$value)){return false;} }\n\t\treturn true;\n\t}\n\n"""
r("\n\tprivate static function reports_page(", helper+"\tprivate static function reports_page(",'helper')
r("private static function reports_page($status,$cursor,$limit){global$wpdb;$t=self::tables();$rows=$wpdb->get_results($wpdb->prepare(\"SELECT id,public_id,report_type,route_key,block_key,locale,reason_code,message,status,resolution,row_version,created_at,updated_at FROM {$t['reports']} WHERE status=%s AND id>%d ORDER BY id ASC LIMIT %d\",$status,absint($cursor),$limit+1),ARRAY_A);$rows=is_array($rows)?$rows:array();", "private static function reports_page($status,$cursor,$limit){global$wpdb;$t=self::tables();$wpdb->last_error='';$rows=$wpdb->get_results($wpdb->prepare(\"SELECT id,public_id,report_type,route_key,block_key,locale,reason_code,message,status,resolution,row_version,created_at,updated_at FROM {$t['reports']} WHERE status=%s AND id>%d ORDER BY id ASC LIMIT %d\",$status,absint($cursor),$limit+1),ARRAY_A);if(''!==(string)$wpdb->last_error||!is_array($rows)){return new WP_Error('gcu_future_reports_page_query_failed',__('Reports could not be read safely.','global-clinic-usp-integration'),array('status'=>503));}", 'reports page')
r("$sql=\"SELECT id,record_type,record_key,locale,region,status,is_public,payload,row_version,review_due_at,updated_at FROM {$t['records']} WHERE $where ORDER BY id ASC LIMIT %d\";$rows=$wpdb->get_results($wpdb->prepare($sql,$args),ARRAY_A);$rows=is_array($rows)?$rows:array();", "$sql=\"SELECT id,record_type,record_key,locale,region,status,is_public,payload,row_version,review_due_at,updated_at FROM {$t['records']} WHERE $where ORDER BY id ASC LIMIT %d\";$wpdb->last_error='';$rows=$wpdb->get_results($wpdb->prepare($sql,$args),ARRAY_A);if(''!==(string)$wpdb->last_error||!is_array($rows)){return new WP_Error('gcu_future_records_page_query_failed',__('Future records could not be read safely.','global-clinic-usp-integration'),array('status'=>503));}", 'records page check')
r("\t\t$status = sanitize_key( $status );\n\t\treturn $wpdb->get_results( $wpdb->prepare( \"SELECT public_id,report_type,route_key,block_key,locale,reason_code,message,status,resolution,row_version,created_at,updated_at FROM {$t['reports']} WHERE status=%s ORDER BY created_at ASC LIMIT %d\", $status, $limit ), ARRAY_A );", "\t\t$status = sanitize_key( $status );\n\t\t$wpdb->last_error='';$rows=$wpdb->get_results( $wpdb->prepare( \"SELECT public_id,report_type,route_key,block_key,locale,reason_code,message,status,resolution,row_version,created_at,updated_at FROM {$t['reports']} WHERE status=%s ORDER BY created_at ASC LIMIT %d\", $status, $limit ), ARRAY_A );\n\t\treturn ''!==(string)$wpdb->last_error||!is_array($rows)?new WP_Error('gcu_future_reports_query_failed',__('Reports could not be read safely.','global-clinic-usp-integration'),array('status'=>503)):$rows;", 'reports read')
r("global$wpdb;$t=self::tables();$row=$wpdb->get_row($wpdb->prepare(\"SELECT * FROM {$t['reports']} WHERE public_id=%s\",$id),ARRAY_A);if(!$row)", "global$wpdb;$t=self::tables();$wpdb->last_error='';$row=$wpdb->get_row($wpdb->prepare(\"SELECT * FROM {$t['reports']} WHERE public_id=%s\",$id),ARRAY_A);if(''!==(string)$wpdb->last_error){return new WP_Error('gcu_future_report_read_failed',__('The report could not be read safely.','global-clinic-usp-integration'),array('status'=>503));}if(!$row)", 'resolve read')
r("$done=$wpdb->query($wpdb->prepare(\"UPDATE {$t['reports']}", "$wpdb->last_error='';$done=$wpdb->query($wpdb->prepare(\"UPDATE {$t['reports']}", 'resolve update clear')
r("$resolution,current_time('mysql',true),(int)$row['id'],(int)$expected));if(1!==$done||false===$repo->audit", "$resolution,current_time('mysql',true),(int)$row['id'],(int)$expected));if(''!==(string)$wpdb->last_error||1!==$done||false===$repo->audit", 'resolve update check')
r("$nonce = wp_nonce_field( 'gcu_future_report', '_wpnonce', true, false );\n\t\treturn '<section", "$nonce = wp_nonce_field( 'gcu_future_report', '_wpnonce', true, false );\n\t\t$report_id=wp_generate_uuid4();\n\t\treturn '<section", 'form id')
r('<input type="hidden" name="action" value="gcu_future_report"><input type="hidden" name="route_key"', """<input type="hidden" name="action" value="gcu_future_report"><input type="hidden" name="report_id" value="' . esc_attr( $report_id ) . '"><input type="hidden" name="route_key""" ,'form hidden')
r("\t\t$data = array(\n\t\t\t'reason_code' =>", "\t\t$data = array(\n\t\t\t'report_id' => isset( $_POST['report_id'] ) ? sanitize_text_field( wp_unslash( $_POST['report_id'] ) ) : '',\n\t\t\t'reason_code' =>", 'submit id')
r("\t\tself::resolve_report_record( $id, $expected, $status, $resolution );\n\t\twp_safe_redirect( admin_url( 'options-general.php?page=global-clinic-usp-future' ) );", "\t\t$result=self::resolve_report_record( $id, $expected, $status, $resolution );\n\t\twp_safe_redirect(add_query_arg('gcu_report_update',is_wp_error($result)?'failed':'updated',admin_url('options-general.php?page=global-clinic-usp-future')));", 'resolve result')
r("\t\t$reports = self::reports( 'open', 50 );\n\t\t$consistency = self::consistency_graph();", "\t\t$reports = self::reports( 'open', 50 );\n\t\tif(is_wp_error($reports)){echo '<div class=\"notice notice-error\"><p>'.esc_html($reports->get_error_message()).'</p></div>';return;}\n\t\t$consistency = self::consistency_graph();", 'admin report read')
p.write_text(s)
